{
  "name": "pears-docs",
  "version": "1.0.0",
  "description": "Documentation for Pears",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "clean": "rm -rf .vitepress/dist .vitepress/cache",
    "deploy": "npm run build && node scripts/deploy.js"
  },
  "devDependencies": {
    "vitepress": "^1.0.0-rc.44"
  }
}
